# Debug partition table — same as partitions_release but with a coredump partition at the end of flash.
# Coredump is placed at the very end of 4MB flash (0x400000 - 256K = 0x3C0000) so it can't be overwritten
# by the application even if it grows large.
# Name,   Type, SubType,  Offset,    Size,   Flags
nvs,      data, nvs,      ,          0x6000,
phy_init, data, phy,      ,          0x1000,
factory,  app,  factory,  ,          2560K,
coredump, data, coredump, 0x3C0000,  256K,
